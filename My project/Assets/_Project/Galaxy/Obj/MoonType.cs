namespace _Project.Galaxy.Obj
{
    public enum MoonType
    {
        Rocky,       // Каменистая (как Луна)
        Icy,         // Ледяная (как Европа, Энцелад)
        Volcanic,    // Вулканическая (как Ио)
        Desert,      // Сухая/пустынная
        Ocean,       // Океаническая (подлёдные океаны)
        Captured // Захваченный астероид
    }
}
