# Решения

## Имена систем (ручные)
- Для ручных имён систем используется поле `StarSys.CustomName`.
- Если `CustomName` задано, оно имеет приоритет над локализованным именем.
- Центральная ЧД получает имя из `GalaxyConstants.CentralBlackHoleName` через `CustomName`.
