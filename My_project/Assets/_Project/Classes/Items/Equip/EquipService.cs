using _Project.Scripts.Ships;

namespace _Project.Items.Equip
{
    public static class EquipService
    {
        // Контракт: экипировка возвращается как набор, без изменения Ship.
        public static EquipSet EquipShip(in Ship ship, int qualityEquip)
        {
            return default;
        }
    }
}
